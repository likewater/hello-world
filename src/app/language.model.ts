export class Language {
  constructor (public language: string, public author: string, public year: number, public type: string, public code: string, public description: string, public link: string) {}
}
